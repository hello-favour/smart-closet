import 'package:flutter/material.dart';

class ProfileAvaters extends StatelessWidget {
  const ProfileAvaters({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}